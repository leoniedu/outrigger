% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_scenarios.R
\name{run_scenarios}
\alias{run_scenarios}
\title{Run multiple rotation scenarios}
\usage{
run_scenarios(
  scenarios,
  distance_km,
  switch_time_min,
  n_pacers = 3,
  n_steerers = 2,
  n_regular = 4,
  steerers_can_paddle_5 = TRUE
)
}
\arguments{
\item{scenarios}{A data frame with columns:
\describe{
\item{scenario_name}{Character. Name for the scenario.}
\item{stint_min}{Numeric. Stint length in minutes.}
\item{speed_kmh}{Numeric. Expected speed in km/h.}
}}

\item{distance_km}{Race distance in kilometers.}

\item{switch_time_min}{Time required for each crew switch in minutes.}

\item{n_pacers}{Number of paddlers who can pace (seats 1-2). Default is 3.}

\item{n_steerers}{Number of paddlers who can steer (seat 6). Default is 2.}

\item{n_regular}{Number of regular paddlers (seats 3-5 only). Default is 4.}

\item{steerers_can_paddle_5}{Logical. Can steerers also paddle in seat 5?
Default is TRUE.}
}
\value{
A tibble with one row per scenario, containing all metrics from
\code{\link[=analyze_scenario]{analyze_scenario()}}. Results have class \code{outrigger_results} for
custom printing.
}
\description{
Analyzes multiple crew rotation scenarios and returns combined results.
This is useful for comparing different stint lengths and speeds.
}
\examples{
# Define scenarios
scenarios <- data.frame(
  scenario_name = c("Conservative", "Moderate", "Aggressive"),
  stint_min = c(60, 50, 30),
  speed_kmh = c(10, 10.5, 11.5)
)

# Run all scenarios
results <- run_scenarios(
  scenarios = scenarios,
  distance_km = 60,
  switch_time_min = 1.5
)

results
}
